#include <stdio.h>
/**
 * main - Entry point
 *
 * for loop
 *
 * Return: Always 0(Succes)
 */
int main(void)
{
	char letter;

	for (ch = 'z' ; ch >= 'a' ; letter--)
	{
		putchar(letter);
	}

	return (0);
}
