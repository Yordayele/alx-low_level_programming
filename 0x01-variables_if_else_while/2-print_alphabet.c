#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 **/   
int (main)
{
	char letter;

	for (int letter = 'a' ; letter = 'z' ; letter++)
		putchar(letter);

	putchar("\n");
	return (0);
}
